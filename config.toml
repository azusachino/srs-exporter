[app]
# 0.0.0.0 for docker
host = "localhost"
port = 9717

[srs]
mode = "edge"
# domain + rtmp_port (external service)
domain = "localhost"
rtmp_port = 1935
# host + http_port (internal service)
host = "localhost"
http_port = 1985

[nacos]
host = "172.31.103.161"
port = 18848
namespace_id = "public"
group_name = "public"
